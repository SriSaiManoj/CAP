class Solution(object):
    def isPalindrome(self, x):
        temp=x
        res=0
        while(x>0):
            n = x%10
            res = res*10 + n
            x = x//10
        if(res==temp):
            return True
        else:
            return False
